# Matching App

A matching application built with React and Snowflake.

## Setup
1. Clone the repository
2. Navigate to frontend: `cd frontend`
3. Install dependencies: `npm install`
4. Start the app: `npm start`
